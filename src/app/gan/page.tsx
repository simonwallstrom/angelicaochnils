// import { getContacts } from '../getanewsletter'

export default async function ContactsPage() {
  // const contacts = await getContacts()

  return (
    <div>
      <h1>Newsletter Contacts</h1>
      {/* <ul>
        {contacts.results.map((contact: any) => (
          <li key={contact.id}>{contact.email}</li>
        ))}
      </ul> */}
    </div>
  )
}
